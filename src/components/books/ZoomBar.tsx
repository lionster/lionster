import {DivAnd, DivComponent} from '../utils';

export const ZoomBar: DivComponent = ({className}) => {
    return (
        <DivAnd className="flex" and={className}>
            ZoomBar
        </DivAnd>
    );
};
